import { BodyNode } from "../../models/nodes";
import { VerificationError } from "../verification-error";

export function verifyBodyNode(body: BodyNode): VerificationError[] {
  const errors: VerificationError[] = [];

  return errors;
}
