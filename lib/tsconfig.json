{
  "extends": "../tsconfig",
  "include": [
    "src",
    // Not sure why, but TypeScript won't compile without this.
    "node_modules/assert-never"
  ],
  "exclude": [
    "examples",
    "**/*.spec.ts"
  ]
}
