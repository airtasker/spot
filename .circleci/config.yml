# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build-lib:
    docker:
      - image: circleci/node:10-stretch

    working_directory: ~/repo/lib

    steps:
      - checkout:
          path: ~/repo

      # Download and cache dependencies
      - restore_cache:
          keys:
          - v1-lib-dependencies-{{ checksum "package.json" }}
          # fallback to using the latest cache if no exact match is found
          - v1-lib-dependencies-

      - run: yarn install

      - save_cache:
          paths:
            - node_modules
          key: v1-lib-dependencies-{{ checksum "package.json" }}

      - run:
          name: Run tests
          command: yarn test

      - run:
          name: Compile
          command: yarn build

  build-cli:
    docker:
      - image: node:latest

    working_directory: ~/repo/cli

    steps:
      - checkout:
          path: ~/repo

      - restore_cache: &restore_cache
          keys:
          - v1-cli-dependencies-{{ checksum "package.json" }}
          # fallback to using the latest cache if no exact match is found
          - v1-cli-dependencies-

      - run:
          name: Install dependencies
          command: yarn

      - save_cache:
          paths:
            - node_modules
          key: v1-cli-dependencies-{{ checksum "package.json" }}

      - run: ./bin/run --version

      - run: ./bin/run --help

      - run:
          name: Testing
          command: yarn test

      - run:
          name: Speccy (single file)
          command: ./bin/run generate --api ../examples/single-file/api.ts -l yaml --generator openapi-3 -o output/ && speccy lint output/api.yaml

      - run:
          name: Speccy (multiple files)
          command: ./bin/run generate --api ../examples/multi-file/api.ts -l yaml --generator openapi-3 -o output/ && speccy lint output/api.yaml

workflows:
  version: 2
  build-and-test:
    jobs:
      - build-lib
      - build-cli
